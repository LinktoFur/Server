version: '3.8'

services:
  linktofur-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:2778:2778"
    volumes:
      - ./config.toml:/app/config.toml
      - ./logs:/app/logs
    restart: unless-stopped
